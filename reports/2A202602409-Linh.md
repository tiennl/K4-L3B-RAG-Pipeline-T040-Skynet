# Báo cáo đóng góp cá nhân

## Thông tin

- Họ và tên: Linh
- Mã học viên: 2A202602409
- Nhóm: K4-L3B
- Repository/branch: `K4-L3B-RAG-Pipeline-T040-Skynet` / `linh`

## Phần việc đã thực hiện

| Module/deliverable | Việc trực tiếp thực hiện | File/commit/PR | Trạng thái |
| --- | --- | --- | --- |
| UI chatbot RAG | Chuyển trải nghiệm tra cứu thành không gian hero gọn, có hội thoại, prompt gợi ý, citation, bộ lọc và feedback. | `app.py`; `8ced612`, `86a6ac3`, `4f83e5e`, `7982d8c`, `ea05618` | Done |
| Sinh câu trả lời có nguồn | Hoàn thiện format context, citation và safe refusal; UI chỉ hiển thị nguồn có evidence. | `src/task10_generation.py`; `895fccd` | Done |
| Golden dataset | Bổ sung 15 câu grounded gồm 13 in-domain và 2 tình huống từ chối an toàn. | `group_project/evaluation/golden_dataset.json`; `364e314` | Done |
| Evaluation D4 | Chạy dense-only so với hybrid+RRF, tạo runner tái lập, artifact và báo cáo A/B. | `scripts/run_ab_eval.py`, `group_project/evaluation/RESULT.md`, `group_project/evaluation/ab_results.json` | Done |
| Demo Z2 | Kiểm tra health Streamlit và ba truy vấn end-to-end bằng pipeline cùng app. | `http://localhost:8501/_stcore/health`; 2026-09-26 | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Dùng `generate_with_citation()` làm điểm vào duy nhất cho UI và chỉ render citation từ `sources`.
   **Lý do/evidence:** Câu trả lời, source và retrieval method đồng bộ; khi thiếu evidence, pipeline trả safe refusal với `sources=[]`.
   **Trade-off:** Chatbot từ chối nhiều hơn chatbot tự do, nhưng tránh hiển thị nguồn không hỗ trợ câu trả lời.

2. **Quyết định:** Giữ hybrid+RRF làm retrieval mặc định.
   **Lý do/evidence:** A/B 15 câu cho average 0.6726, cao hơn dense-only 0.6280; context recall tăng 0.7000 lên 0.7667.
   **Trade-off:** Thêm BM25/RRF cục bộ để đổi lấy coverage evidence tốt hơn.

## Kiểm thử và kết quả

- `pytest -q`: 43 passed.
- In-domain dễ: doanh thu 450 triệu/năm không phải nộp GTGT; trả lời có 5 sources, retrieval `hybrid`.
- In-domain khó: hộ khoán năm 2025, doanh thu không quá 500 triệu; trả lời thông báo doanh thu năm 2026, có 5 sources.
- Out-of-domain: “Cách nướng cá basa bằng nồi chiên không dầu?”; trả safe refusal, `sources=[]`, `retrieval_source=none`.
- Lỗi phát hiện: câu ranh giới doanh thu đúng 500 triệu TNCN đôi lúc bị model từ chối dù corpus có evidence; đã ghi thành ưu tiên regression trong evaluation.

## Điều còn hạn chế

- RAGAS grounded metrics chấm 0 cho safe refusal đúng khi context thuế không liên quan câu hỏi ngoài domain.
- Nếu có thêm thời gian: thêm metric cho safe refusal và regression test cho mốc doanh thu đúng 500 triệu.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh phần việc có thể chạy lại và đối chiếu bằng file, commit, test hoặc artifact evaluation.

- Ngày: 2026-09-26
- Tên thành viên: Linh
